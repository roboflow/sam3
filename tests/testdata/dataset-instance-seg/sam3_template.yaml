# @package _global_
defaults:
  - _self_

# ============================================================================
# Paths Configuration (Chage this to your own paths)
# ============================================================================
paths:
  checkpoint_path: /checkpoint/sam3/weights.pt
  experiment_log_dir: {{cache_path}}/sam3_logs

# Roboflow dataset configuration
roboflow_train:
  num_images: {{num_images}}  # Actual count from dataset
  supercategory: {{supercategory}}  # Extracted from dataset

  # Training transforms pipeline
  train_transforms:
    - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
      transforms:
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterCrowds
        - _target_: sam3.train.transforms.point_sampling.RandomizeInputBbox
          box_noise_std: 0.1
          box_noise_max: 20
        - _target_: sam3.train.transforms.segmentation.DecodeRle
        - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
          sizes:
            _target_: sam3.train.transforms.basic.get_random_resize_scales
            size: ${scratch.resolution}
            min_size: 480
            rounded: false
          max_size:
            _target_: sam3.train.transforms.basic.get_random_resize_max_size
            size: ${scratch.resolution}
          square: true
          consistent_transform: ${scratch.consistent_transform}
        - _target_: sam3.train.transforms.basic_for_api.PadToSizeAPI
          size: ${scratch.resolution}
          consistent_transform: ${scratch.consistent_transform}
        - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterEmptyTargets
        - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
          mean: ${scratch.train_norm_mean}
          std: ${scratch.train_norm_std}
        - _target_: sam3.train.transforms.mdetr.ApplyPosEmbedToBoxesAPI
          pos_embed: ${scratch.pos_embed}
        - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
          query_filter:
            _target_: sam3.train.transforms.filter_query_transforms.FilterEmptyTargets
    - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
      query_filter:
        _target_: sam3.train.transforms.filter_query_transforms.FilterFindQueriesWithTooManyOut
        max_num_objects: ${scratch.max_ann_per_img}

  # Validation transforms pipeline
  val_transforms:
    - _target_: sam3.train.transforms.filter_query_transforms.FlexibleFilterFindGetQueries
      query_filter:
          _target_: sam3.train.transforms.filter_query_transforms.DeleteContextQueries
    - _target_: sam3.train.transforms.filter_query_transforms.DropGeoPromptsInLastStage
    - _target_: sam3.train.transforms.basic_for_api.ComposeAPI
      transforms:
        - _target_: sam3.train.transforms.basic_for_api.RandomResizeAPI
          sizes: ${scratch.resolution}
          max_size:
            _target_: sam3.train.transforms.basic.get_random_resize_max_size
            size: ${scratch.resolution}
          square: true
          consistent_transform: False
        - _target_: sam3.train.transforms.basic_for_api.ToTensorAPI
        - _target_: sam3.train.transforms.basic_for_api.NormalizeAPI
          # mean: [0.485, 0.456, 0.406]
          # std: [0.229, 0.224, 0.225]
          mean: ${scratch.train_norm_mean}
          std: ${scratch.train_norm_std}
    - _target_: sam3.train.transforms.mdetr.ApplyPosEmbedToBoxesAPI
      pos_embed: ${scratch.pos_embed}

  # loss config (no mask loss)
  roboflow_loss:
    _target_: sam3.train.loss.sam3_loss.Sam3LossWrapper
    matcher: ${scratch.matcher}
    o2m_weight: 2.0
    o2m_matcher:
      _target_: sam3.train.matcher.BinaryOneToManyMatcher
      alpha: 0.3
      threshold: 0.4
      topk: 4
    use_o2m_matcher_on_o2m_aux: false
    loss_fns_find:
      - _target_: sam3.train.loss.loss_fns.Boxes
        weight_dict:
          loss_bbox: 5.0
          loss_giou: 2.0
      - _target_: sam3.train.loss.loss_fns.IABCEMdetr
        weak_loss: False
        weight_dict:
          loss_ce: 100
        pos_weight: 10
        gamma: 2
    scale_by_find_batch_size: ${scratch.scale_by_find_batch_size}

  # Loss to use for segmentation masks
  # loss:
  #   all:
  #     _target_: sam3.train.loss.sam3_loss.Sam3LossWrapper
  #     matcher: ${scratch.matcher}
  #     o2m_weight: 2.0
  #     o2m_matcher:
  #       _target_: sam3.train.matcher.BinaryOneToManyMatcher
  #       alpha: 0.3
  #       threshold: 0.4
  #       topk: 4
  #     # use one-to-one (instead of one-to-many) matcher on o2m auxiliary outputs (to be consistent with FA 1a_v4.5)
  #     use_o2m_matcher_on_o2m_aux: false
  #     loss_fns_find:
  #       - _target_: osam3.train.loss.loss_fns.Boxes
  #         weight_dict:
  #           loss_bbox: 5.0
  #           loss_giou: 2.0
  #       - _target_: sam3.train.loss.loss_fns.IABCEMdetr
  #         weak_loss: False
  #         weight_dict:
  #           loss_ce: 100
  #         pos_weight: 10
  #         gamma: 2
  #         pad_n_queries: ${scratch.num_queries}
  #       - _target_: sam3.train.loss.loss_fns.Masks
  #         focal_alpha: ${scratch.segmentation_focal_alpha}
  #         focal_gamma: ${scratch.segmentation_focal_gamma}
  #         weight_dict:
  #           loss_mask: ${scratch.loss_mask}
  #           loss_dice: ${scratch.loss_dice}
  #         compute_aux: ${trainer.model.segmentation_head.aux_masks}
  #     loss_fn_semantic_seg:
  #       _target_: sam3.train.loss.loss_fns.SemanticSegCriterion
  #       presence_head: True
  #       focal: True
  #       focal_alpha: ${scratch.sem_seg_focal_alpha}
  #       focal_gamma: ${scratch.sem_seg_focal_gamma}
  #       downsample: False
  #       weight_dict:
  #         loss_semantic_seg: ${scratch.sem_seg_weight}
  #         loss_semantic_presence: ${scratch.sem_seg_presence_weight}
  #         loss_semantic_dice: ${scratch.sem_seg_dice_weight}
  #     scale_by_find_batch_size: ${scratch.scale_by_find_batch_size}

# ============================================================================
# Different helper parameters and functions
# ============================================================================
scratch:
  # Model parameters
  d_model: 256
  input_box_embedding_dim: ${add:${scratch.d_model},2}
  pos_embed:
    _target_: sam3.model.position_encoding.PositionEmbeddingSine
    num_pos_feats: ${scratch.d_model}
    normalize: true
    scale: null
    temperature: 10000

  # Box processing
  use_presence_eval: False
  original_box_postprocessor:
    _target_: sam3.train.eval.postprocessors.PostProcessAPI
    soft_token: true
    focal_loss: true
    max_dets_per_img: -1  # infinite detections
    use_original_ids: true
    use_original_sizes: true
    # TODO: (Kalyan) Add support for presence token!
    # use_presence: ${scratch.use_presence_eval}

  # Matcher configuration
  matcher:
    _target_: sam3.train.matcher.BinaryHungarianMatcherV2
    focal: true  # with `focal: true` it is equivalent to BinaryFocalHungarianMatcher
    cost_class: 2.0
    cost_bbox: 5.0
    cost_giou: 2.0
    alpha: 0.25
    gamma: 2
    stable: False
  scale_by_find_batch_size: True

  # Image processing parameters
  resolution: 1008
  consistent_transform: False
  max_ann_per_img: 200

  # Normalization parameters
  train_norm_mean: [0.5, 0.5, 0.5]
  train_norm_std: [0.5, 0.5, 0.5]
  val_norm_mean: [0.5, 0.5, 0.5]
  val_norm_std: [0.5, 0.5, 0.5]

  # Training parameters
  train_batch_size: 1
  val_batch_size: 1
  num_train_workers: 0  # Reduce workers to avoid shared memory issues
  num_val_workers: 0
  max_data_epochs: 20
  target_epoch_size: 1500
  hybrid_repeats: 1
  context_length: 2
  gather_pred_via_filesys: false

  # Learning rate and scheduler parameters
  lr_scale: 0.1
  lr_transformer: ${times:8e-4,${scratch.lr_scale}}
  lr_vision_backbone: ${times:2.5e-4,${scratch.lr_scale}}
  lr_language_backbone: ${times:5e-5,${scratch.lr_scale}}
  lrd_vision_backbone: 0.9
  wd: 0.1
  scheduler_timescale: 20
  scheduler_warmup: 20
  scheduler_cooldown: 20

# ============================================================================
# Trainer Configuration
# ============================================================================

trainer:

  # _target_: sam3.train.trainer.Trainer
  _target_: sam3.train.trainer.Trainer
  skip_saving_ckpts: false  # Enable checkpoint saving
  empty_gpu_mem_cache_after_eval: True
  skip_first_val: True
  max_epochs: {{max_epochs}} # Just 3 epochs for testing
  accelerator: cuda
  seed_value: 123
  val_epoch_freq: 1
  mode: train

  distributed:
    backend: nccl
    find_unused_parameters: True
    gradient_as_bucket_view: True

  loss:
    all: ${roboflow_train.roboflow_loss}
    default:
      _target_: sam3.train.loss.sam3_loss.DummyLoss

  data:
    train:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.sam3_image_dataset.Sam3ImageDataset
        limit_ids: ${roboflow_train.num_images}
        transforms: ${roboflow_train.train_transforms}
        load_segmentation: false
        max_ann_per_img: 500000
        multiplier: 1
        max_train_queries: 50000
        max_train_get_queries: 50000
        max_val_queries: 50000
        max_val_get_queries: 50000
        training: true
        use_caching: False
        img_folder: /workspace/tests/testdata/dataset-instance-seg/train/
        ann_file: /workspace/tests/testdata/dataset-instance-seg/train/_annotations.coco.json

      shuffle: True
      batch_size: ${scratch.train_batch_size}
      num_workers: ${scratch.num_train_workers}
      pin_memory: True
      drop_last: True
      collate_fn:
        _target_: sam3.train.data.collator.collate_fn_api
        _partial_: true
        repeats: ${scratch.hybrid_repeats}
        dict_key: all
        input_box_embedding_dim: ${scratch.input_box_embedding_dim}
        with_seg_masks: true

    val:
      _target_: sam3.train.data.torch_dataset.TorchDataset
      dataset:
        _target_: sam3.train.data.sam3_image_dataset.Sam3ImageDataset
        coco_json_loader:
          _target_: sam3.train.data.coco_json_loaders.COCO_EVAL_API_FROM_JSON_BOX_ONLY
          _partial_: true
        img_folder: /workspace/tests/testdata/dataset-instance-seg/test/
        ann_file: /workspace/tests/testdata/dataset-instance-seg/test/_annotations.coco.json
        transforms: ${roboflow_train.val_transforms}
        max_ann_per_img: 100000
        multiplier: 1
        training: false

      shuffle: False
      batch_size: ${scratch.val_batch_size}
      num_workers: ${scratch.num_val_workers}
      pin_memory: True
      drop_last: False
      collate_fn:
        _target_: sam3.train.data.collator.collate_fn_api
        _partial_: true
        input_box_embedding_dim: ${scratch.input_box_embedding_dim}
        repeats: ${scratch.hybrid_repeats}
        dict_key: roboflow100

  # TODO (Kalyan): Expose more model parameters, droppath rate, dropout, etc
  # TODO (Kalyan): Add support for presence token.
  model:
    _target_: sam3.model_builder.build_sam3_image_model
    bpe_path: {{bpe_path}}
    device: cuda  # We have GPU
    eval_mode: false
    enable_segmentation: false # Warning: Enable this if using segmentation.
    checkpoint_path: ${paths.checkpoint_path}

  meters:
    val:
      roboflow100:
        detection:
          _target_: sam3.train.eval.coco_eval_offline.CocoEvaluatorOffline
          iou_type: "bbox"
          tide: False
          dump_dir: ${launcher.experiment_log_dir}/dumps/roboflow/${roboflow_train.supercategory}
          gt_path: /workspace/tests/testdata/dataset-instance-seg/test/_annotations.coco.json
          postprocessor: ${scratch.original_box_postprocessor}
          gather_pred_via_filesys: ${scratch.gather_pred_via_filesys}

  optim:
    amp:
      enabled: True
      amp_dtype: bfloat16

    optimizer:
      _target_: torch.optim.AdamW

    gradient_clip:
      _target_: sam3.train.optim.optimizer.GradientClipper
      max_norm: 0.1
      norm_type: 2

    param_group_modifiers:
      - _target_: sam3.train.optim.optimizer.layer_decay_param_modifier
        _partial_: True
        layer_decay_value: ${scratch.lrd_vision_backbone}
        apply_to: 'backbone.vision_backbone.trunk'
        overrides:
          - pattern: '*pos_embed*'
            value: 1.0

    options:
      lr:
        - scheduler:  # transformer and class_embed
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_transformer}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_vision_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.vision_backbone.*'
        - scheduler:
            _target_: sam3.train.optim.schedulers.InverseSquareRootParamScheduler
            base_lr: ${scratch.lr_language_backbone}
            timescale: ${scratch.scheduler_timescale}
            warmup_steps: ${scratch.scheduler_warmup}
            cooldown_steps: ${scratch.scheduler_cooldown}
          param_names:
            - 'backbone.language_backbone.*'

      weight_decay:
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: ${scratch.wd}
        - scheduler:
            _target_: fvcore.common.param_scheduler.ConstantParamScheduler
            value: 0.0
          param_names:
            - '*bias*'
          module_cls_names: ['torch.nn.LayerNorm']

  checkpoint:
    save_dir: ${launcher.experiment_log_dir}/checkpoints
    save_freq: 0  # Save checkpoint every epoch

  logging:
    tensorboard_writer:
      _target_: sam3.train.utils.logger.make_tensorboard_logger
      log_dir: ${launcher.experiment_log_dir}/tensorboard
      flush_secs: 120
      should_log: True
    wandb_writer: null
    log_dir: ${launcher.experiment_log_dir}/logs/${roboflow_train.supercategory}
    log_freq: 10

# ============================================================================
# Launcher and Submitit Configuration
# ============================================================================

launcher:
  num_nodes: 1
  gpus_per_node: 1  # SAM3 expects exactly 1 GPU for single node runner
  experiment_log_dir: ${paths.experiment_log_dir}
  multiprocessing_context: forkserver

submitit:
  account: null
  partition: null
  qos: null
  timeout_hour: 72
  use_cluster: True
  cpus_per_task: 10
  port_range: [10000, 65000]
  constraint: null #"volta32gb"
  # Uncomment for job array configuration
  job_array:
    num_tasks: 100
    task_index: 0

# ============================================================================
# Available Roboflow Supercategories (for reference)
# ============================================================================

all_roboflow_supercategories:
  - -grccs
  - zebrasatasturias
  - cod-mw-warzone
  - canalstenosis
  - label-printing-defect-version-2
  - new-defects-in-wood
  - orionproducts
  - aquarium-combined
  - varroa-mites-detection--test-set
  - clashroyalechardetector
  - stomata-cells
  - halo-infinite-angel-videogame
  - pig-detection
  - urine-analysis1
  - aerial-sheep
  - orgharvest
  - actions
  - mahjong
  - liver-disease
  - needle-base-tip-min-max
  - wheel-defect-detection
  - aircraft-turnaround-dataset
  - xray
  - wildfire-smoke
  - spinefrxnormalvindr
  - ufba-425
  - speech-bubbles-detection
  - train
  - pill
  - truck-movement
  - car-logo-detection
  - inbreast
  - sea-cucumbers-new-tiles
  - uavdet-small
  - penguin-finder-seg
  - aerial-airport
  - bibdetection
  - taco-trash-annotations-in-context
  - bees
  - recode-waste
  - screwdetectclassification
  - wine-labels
  - aerial-cows
  - into-the-vale
  - gwhd2021
  - lacrosse-object-detection
  - defect-detection
  - dataconvert
  - x-ray-id
  - ball
  - tube
  - 2024-frc
  - crystal-clean-brain-tumors-mri-dataset
  - grapes-5
  - human-detection-in-floods
  - buoy-onboarding
  - apoce-aerial-photographs-for-object-detection-of-construction-equipment
  - l10ul502
  - floating-waste
  - deeppcb
  - ism-band-packet-detection
  - weeds4
  - invoice-processing
  - thermal-cheetah
  - tomatoes-2
  - marine-sharks
  - peixos-fish
  - sssod
  - aerial-pool
  - countingpills
  - asphaltdistressdetection
  - roboflow-trained-dataset
  - everdaynew
  - underwater-objects
  - soda-bottles
  - dentalai
  - jellyfish
  - deepfruits
  - activity-diagrams
  - circuit-voltages
  - all-elements
  - macro-segmentation
  - exploratorium-daphnia
  - signatures
  - conveyor-t-shirts
  - fruitjes
  - grass-weeds
  - infraredimageofpowerequipment
  - 13-lkc01
  - wb-prova
  - flir-camera-objects
  - paper-parts
  - football-player-detection
  - trail-camera
  - smd-components
  - water-meter
  - nih-xray
  - the-dreidel-project
  - electric-pylon-detection-in-rsi
  - cable-damage
